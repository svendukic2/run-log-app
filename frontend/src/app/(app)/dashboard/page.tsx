// Placeholder view: the full dashboard (header, goal card, charts) is built in
// later tickets (RUN-15+). RUN-12 only needs the route so sidebar navigation works.
export default function DashboardPage() {
  return (
    <div className="px-[40px] py-[32px]">
      <h1 className="font-display text-[30px] font-bold tracking-[-0.6px] text-text-primary">
        Dashboard
      </h1>
    </div>
  );
}
