{
  "name": "e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end acceptance tests per sprint (Playwright), separate from app deps",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc -p ."
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
